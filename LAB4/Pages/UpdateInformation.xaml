<Window
    Height="300"
    Title="Update Information"
    Width="350"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d"
    x:Class="lab4_2.Pages.UpdateInformation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:lab4_2.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <ResourceDictionary Source="../styles.xaml" />
    </Window.Resources>
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Name  -->
        <TextBlock
            Grid.Column="0"
            Grid.Row="0"
            Style="{StaticResource LabelStyle}"
            Text="Ім'я"
            VerticalAlignment="Center" />
        <TextBox
            Grid.Column="1"
            Grid.Row="0"
            Margin="10,5"
            Text="{Binding Name}"
            Width="200" />

        <!--  Rating  -->
        <TextBlock
            Grid.Column="0"
            Grid.Row="1"
            Style="{StaticResource LabelStyle}"
            Text="Рейтинг"
            VerticalAlignment="Center" />
        <TextBox
            Grid.Column="1"
            Grid.Row="1"
            Margin="10,5"
            Text="{Binding Rating}"
            Width="200" />

        <!--  University  -->
        <TextBlock
            Grid.Column="0"
            Grid.Row="2"
            Style="{StaticResource LabelStyle}"
            Text="Університет"
            VerticalAlignment="Center" />
        <TextBox
            Grid.Column="1"
            Grid.Row="2"
            Margin="10,5"
            Text="{Binding University}"
            Width="200" />

        <!--  Specialization  -->
        <TextBlock
            Grid.Column="0"
            Grid.Row="3"
            Style="{StaticResource LabelStyle}"
            Text="Спеціальність"
            VerticalAlignment="Center" />
        <TextBox
            Grid.Column="1"
            Grid.Row="3"
            Margin="10,5"
            Text="{Binding Specialization}"
            Width="200" />

        <!--  Email  -->
        <TextBlock
            Grid.Column="0"
            Grid.Row="4"
            Style="{StaticResource LabelStyle}"
            Text="Пошта"
            VerticalAlignment="Center" />
        <TextBox
            Grid.Column="1"
            Grid.Row="4"
            Margin="10,5"
            Text="{Binding Email}"
            Width="200" />

        <!--  OK Button  -->
        <Button
            Click="OkButton_Click"
            Content="OK"
            Grid.ColumnSpan="2"
            Grid.Row="5"
            Height="40"
            HorizontalAlignment="Center"
            Margin="0,20"
            Width="100"
            x:Name="OkButton" />
    </Grid>
</Window>
